# S3 buckets creation module

# This module setup the Media bucket for WordPress media files (to be activated by installing
# a WordPress plugin - WP Offload Media)

# Data sources to setup the bucket name
data "aws_caller_identity" "current" {}
data "aws_region" "current" {}

locals {
  account_id = data.aws_caller_identity.current.account_id
  region     = data.aws_region.current.region

  media_bucket_name = "${var.name_prefix}-media-${local.account_id}"
}

resource "aws_s3_bucket" "media" {
    bucket = local.media_bucket_name
    force_destroy = var.force_destroy

    tags = merge(var.common_tags, {Name = local.media_bucket_name})
}

resource "aws_s3_bucket_public_access_block" "media" {
  bucket = aws_s3_bucket.media.id

  block_public_acls       = true
  block_public_policy     = true
  ignore_public_acls      = true
  restrict_public_buckets = true
}

resource "aws_s3_bucket_ownership_controls" "media" {
  bucket = aws_s3_bucket.media.id

  rule {
    object_ownership = "BucketOwnerEnforced"
  }
}

resource "aws_s3_bucket_versioning" "media" {
  bucket = aws_s3_bucket.media.id

  versioning_configuration {
    status = "Enabled"
  }
}

resource "aws_s3_bucket_server_side_encryption_configuration" "media" {
  bucket = aws_s3_bucket.media.id

  rule {
    apply_server_side_encryption_by_default {
      sse_algorithm = "AES256"
    }
  }
}