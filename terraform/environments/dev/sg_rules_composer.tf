# PLACEHOLDER: add here the security group rules for:
# - EFS Security Group (it needs ECS module)
# - Database (Aurora) Security Group (it needs ECS module)
# - Cache (Valkey Serverless) Security Group (it needs ECS module)